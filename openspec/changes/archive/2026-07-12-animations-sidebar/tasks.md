## 1. Sidebar transition foundation

- [x] 1.1 Add CSS transitions for the sidebar shell so width and spacing animate when the `sidebar-collapsed` class changes.
- [x] 1.2 Update the collapsed-state content rules so the sidebar header, navigation, and footer can fade or slide out instead of disappearing instantly.

## 2. Accessibility and behavior checks

- [x] 2.1 Add reduced-motion overrides so the sidebar still changes state without pronounced animation when motion is disabled.
- [x] 2.2 Verify the animation rules do not interfere with the existing mobile drawer behavior or the saved desktop collapse state.